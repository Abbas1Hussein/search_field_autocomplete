name: search_field_autocomplete
description:  search field with autocomplete suggestions.
version: 0.0.4
homepage: https://github.com/Abbas1Hussein/search_field_autocomplete

environment:
  sdk: '>=3.1.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter: